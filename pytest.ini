[pytest]
pythonpath = .
addopts = -ra
markers =
    unit: pure unit tests, no IO
    integration: HTTP tests with TestClient, no real DB
    sql: end-to-end tests hitting the real Postgres
asyncio_mode = auto